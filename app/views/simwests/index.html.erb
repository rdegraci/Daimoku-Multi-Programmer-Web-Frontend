<h1>Listing simwests</h1>

<table>
  <tr>
  </tr>

<% @simwests.each do |simwest| %>
  <tr>
    <td><%= link_to 'Show', simwest %></td>
    <td><%= link_to 'Edit', edit_simwest_path(simwest) %></td>
    <td><%= link_to 'Destroy', simwest, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New simwest', new_simwest_path %>