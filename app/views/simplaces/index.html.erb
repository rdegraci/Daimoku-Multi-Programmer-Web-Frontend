<h1>Listing simplaces</h1>

<table>
  <tr>
  </tr>

<% @simplaces.each do |simplace| %>
  <tr>
    <td><%= link_to 'Show', simplace %></td>
    <td><%= link_to 'Edit', edit_simplace_path(simplace) %></td>
    <td><%= link_to 'Destroy', simplace, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New simplace', new_simplace_path %>